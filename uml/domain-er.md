# Domain ER (legacy pointer)

The **complete, 1:1 database schema** (all tables, columns, FKs, migration columns) lives in:

**[database-schema.md](./database-schema.md)**

That file replaces the old simplified diagram.
